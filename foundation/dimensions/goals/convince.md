---
id: convince
provenance: framework
schema_version: 1
source_selection:
  require:
    - traceability is true
  prefer:
    - authoritative: +2
---

# convince — Goal entry (framework default)

`kind: strategic` (the schema floor): the artifact's outcome is that the reader comes
to SHARE A POSITION (design §5.2). Persuasion raises the grounding bar, so this entry
carries the design's own example clause contribution (§6.3: "convince -> prefer
authoritative +2; require traceability"):

- **require `traceability is true`** — a hard clause (§6.3): a persuasive piece cites
  only claims with resolvable anchors (§6.5 citability); uncitable facts are excluded
  from its grounding.
- **prefer `authoritative: +2`** — a soft weight nudge (§6.3: prefer never excludes;
  §12.7 CA10: it adjusts the workspace baseline only and never moves an explicit
  user weight at recipe/run).

The clauses ride M3's goal-implied layer (§12.1); a later layer may tighten or —
warned, most-local-wins — relax them (§6.4 SM3). Clauses reference score
characteristics only, never source ids (§6.3).
