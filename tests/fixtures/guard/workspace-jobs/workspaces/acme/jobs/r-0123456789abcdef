{"note": "Obviously-synthetic negative guard fixture (tests/fixtures/guard): a tracked DR-1 async job record under a client workspace (workspaces/acme/jobs/ — the plan's own synthetic example). §22.7-class lossy bookkeeping; the DATA is instance-scoped client data and is gitignored (workspaces/*/jobs/). If ever force-tracked it MUST be flagged as LEAK[workspace-content] (client isolation, CLAUDE.md rules 2/4). Never real client data."}
