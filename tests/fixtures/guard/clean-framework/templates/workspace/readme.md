# Synthetic guard fixture: a GENERIC file in the re-homed framework workspace blueprint
# (templates/workspace/, §23). The blueprint subtree is SCANNED by the [W8] templates/* arm
# (not exempt wholesale); this file carries no x-/provenance:instance signal, so it PASSES.
