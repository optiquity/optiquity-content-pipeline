# Synthetic guard fixture: a `*.template.*` file. Templates are exempt BY NAME (PA-1a)
# from the provenance requirement and the leak scan — this file deliberately carries no
# YAML frontmatter and no provenance line, and the guard must stay green.
