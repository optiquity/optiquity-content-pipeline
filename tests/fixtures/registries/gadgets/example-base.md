---
id: example-base
provenance: framework
schema_version: 2
---

Minimal obviously-generic fixture entry: it sets no attributes, so every value rides the
schema-default floor (design §12.2 L0). Its stamp (2) trails the fixture schema_version (3)
on purpose — a step-12 drift-input demonstration with nothing set, hence no drift.
