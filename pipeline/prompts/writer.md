<!--
STUB — pipeline/prompts/writer.md (plan step 23, decision T9).

This is a placeholder, NOT a filled prompt contract. It exists so
`pipeline.prompts.load_template("writer")` has something to discover — the loader is
content-blind (docs/design.md §21.9: transport/packaging only, never prompt semantics)
and must never fail on a stub like this one.

The writer-stage prompt contract (grounded facts + effective values + Format part
structure + roster context slot; the IR JSON output contract; bounded re-ask on a
contract violation) is ratified and lands HERE, replacing this stub in place, at plan
step 24 (design §15 — the IR + the writer stage). Do not treat this placeholder as the
contract, and do not invent one ahead of step 24.
-->
