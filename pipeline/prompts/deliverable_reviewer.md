<!--
STUB — pipeline/prompts/deliverable_reviewer.md (plan step 23, decision T9).

This is a placeholder, NOT a filled prompt contract. It exists so
`pipeline.prompts.load_template("deliverable_reviewer")` has something to discover — the
loader is content-blind (docs/design.md §21.9: transport/packaging only, never prompt
semantics) and must never fail on a stub like this one.

The deliverable-review prompt contract (§19: FULL, every deliverable incl. every fit/
serialize revision — substance + §16 fidelity + provenance bindings at the IR-fitted/AST
layer + hard limits + format/splits) is ratified and lands HERE, replacing this stub in
place, at plan step 30 (design §19 — review gates). Do not treat this placeholder as the
contract, and do not invent one ahead of step 30.
-->
