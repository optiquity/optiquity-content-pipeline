<!--
STUB — pipeline/prompts/reconciler.md (plan step 23, decision T9).

This is a placeholder, NOT a filled prompt contract. It exists so
`pipeline.prompts.load_template("reconciler")` has something to discover — the loader is
content-blind (docs/design.md §21.9: transport/packaging only, never prompt semantics)
and must never fail on a stub like this one.

The reconciler-stage prompt contract (the §16 fidelity contract: provenance bindings
re-anchored — every ledger fact-id present or explicitly dropped-as-lead; tier promotion
impossible; voice/content parameters echoed; reshape per strategy adapt|split|pass|
truncate) is ratified and lands HERE, replacing this stub in place, at plan step 25
(design §16 — the reconcile pass core). Do not treat this placeholder as the contract,
and do not invent one ahead of step 25.
-->
